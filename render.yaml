databases:
  - name: mayank-rawat-db
    databaseName: portfolio
    user: portfolio

services:
  # Backend API Service
  - type: web
    name: mayank-rawat-api
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mayank-rawat-db
          property: connectionString
      - key: CORS_ORIGINS
        sync: false
    healthCheckPath: /api/health
    
  # Frontend Service
  - type: web
    name: mayankrawat26
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
